---
description:
globs:
alwaysApply: false
---
# 样式统一管理规则

## 核心原则
- **组件内禁止写样式**：所有样式必须在 `src/styles/` 目录中统一管理
- **使用预定义的CSS类**：优先使用 [common.css](mdc:src/styles/common.css) 中定义的工具类
- **主题变量使用**：使用CSS变量定义的主题色彩和尺寸值

## 样式文件组织
```
src/styles/
├── common.css      # 基础样式、变量定义、工具类
├── index.css       # 入口样式文件
├── newtab.css      # 新标签页专用样式
└── sidebar-override.css  # 侧边栏样式覆盖
```

## 组件样式开发规范

### ❌ 禁止行为
```vue
<!-- 禁止在组件中使用 style 标签 -->
<style scoped>
.my-component {
  color: red;
  margin: 10px;
}
</style>

<!-- 禁止使用内联样式 -->
<div style="color: red; margin: 10px;">Content</div>
```

### ✅ 推荐做法
```vue
<!-- 使用预定义的工具类 -->
<template>
  <div class="flex items-center justify-between p-4 mb-3 rounded shadow-sm">
    <h1 class="text-lg font-semibold text-primary">{{ title }}</h1>
    <n-button class="ml-2">Action</n-button>
  </div>
</template>

<!-- 如需特定样式，在 common.css 中定义 -->
<template>
  <div class="bookmark-card">
    <div class="bookmark-header">
      <!-- content -->
    </div>
  </div>
</template>
```

## 工具类使用指南

### 布局类
- `flex`, `flex-col`, `flex-row` - Flexbox 布局
- `items-center`, `items-start`, `items-end` - 垂直对齐
- `justify-center`, `justify-between`, `justify-around` - 水平对齐
- `w-full`, `h-full` - 宽高设置

### 间距类
- `m-0` ~ `m-6` - margin 外边距
- `p-0` ~ `p-6` - padding 内边距
- `mt-`, `mb-`, `ml-`, `mr-` - 方向性外边距
- `pt-`, `pb-`, `pl-`, `pr-` - 方向性内边距

### 文本类
- `text-center`, `text-left`, `text-right` - 文本对齐
- `text-xs`, `text-sm`, `text-base`, `text-lg` - 字体大小
- `font-light`, `font-normal`, `font-medium`, `font-semibold` - 字重
- `text-primary`, `text-success`, `text-warning`, `text-error` - 主题色

### 外观类
- `bg-primary`, `bg-success`, `bg-warning` - 背景色
- `border`, `border-t`, `border-b` - 边框
- `rounded`, `rounded-sm`, `rounded-full` - 圆角
- `shadow-sm`, `shadow-md`, `shadow-lg` - 阴影

## 添加新样式的流程

1. **检查现有工具类**：先查看 [common.css](mdc:src/styles/common.css) 是否已有合适的类
2. **使用 CSS 变量**：新样式应使用预定义的 CSS 变量
3. **添加到 common.css**：如果需要新样式，添加到相应的分类中
4. **遵循命名规范**：使用语义化的类名

## CSS 变量使用

### 颜色变量
```css
color: var(--primary-color);
background-color: var(--card-color);
border-color: var(--border-color);
```

### 尺寸变量
```css
border-radius: var(--border-radius);
font-size: var(--font-size);
height: var(--height-medium);
```

### 阴影和动画
```css
box-shadow: var(--box-shadow-1);
transition: var(--n-duration-medium) var(--n-bezier);
```

## 响应式设计

使用预定义的响应式类：
```html
<div class="mobile:hidden tablet:block desktop:flex">
  <!-- 在不同屏幕尺寸下的显示方式 -->
</div>
```

## 代码检查要点

开发时需要检查：
- [ ] 组件文件中是否有 `<style>` 标签
- [ ] 是否使用了内联样式 `style="..."`
- [ ] 是否使用了预定义的工具类
- [ ] 新样式是否添加到了 common.css 中
- [ ] 是否使用了 CSS 变量而非硬编码值

## 参考文件
- [common.css](mdc:src/styles/common.css) - 主要样式文件
- [index.css](mdc:src/styles/index.css) - 样式入口
- [Header.vue](mdc:src/components/Header.vue) - 良好的组件样式使用示例
- [BookmarkGrid.vue](mdc:src/components/BookmarkGrid.vue) - 组件样式参考
